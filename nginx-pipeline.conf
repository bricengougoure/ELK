input {
  beats {
    port => 5044
    ssl_enabled => false
  }
}

# Optional: timestamp parsing (uncomment and adjust if your events have a "timestamp" field)
# filter {
#   date {
#     match => ["timestamp", "MMM  d HH:mm:ss", "MMM dd HH:mm:ss", "ISO8601"]
#     target => "@timestamp"
#     remove_field => ["timestamp"]
#   }
# }

output {
  elasticsearch {
    hosts => ["https://127.0.0.1:9200"]
    user => "logstash_system"
    password => "Lundi2020"
    ssl_enabled => true
    ssl_certificate_authorities => ["/etc/logstash/certs/http_ca.crt"]
    index => "nginx-%{[event][category]}-%{+YYYY.MM.dd}"
    data_stream => false
  }
}
